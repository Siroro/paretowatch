# ParetoWatch v0.12.1

## Maintenance

- Updated GitHub Actions dependencies to Node.js 24-compatible releases, removing the runner deprecation warnings from CI and release jobs.
- Bumped the patch version while preserving the v0.12 alert and notification feature set.

## Validation

The v0.12.1 maintenance update preserves the existing formatting, locked check, locked test, Clippy, and locked build validation. GitHub release packages continue to be produced for Windows and Linux by the tag workflow.
